# iPaper 1.13.7

Web 1.13.7 修复笔记在切换视图、处理冲突和等待限流时的保存问题，并将摘录回访绑定到实际来源。两个 Worker 复用 1.2.0，无数据库结构变化。

## 变化

- 笔记按账号与论文保留同一个保存会话。切换页签、收起侧栏或切换原译文后，未决冲突仍须明确选择，不会自动用本地稿覆盖服务器。
- 冲突决定只确认点击时提交的版本；等待期间输入的新内容继续保留并在确认后保存。面板和编辑器中的决定按钮使用同一队列。
- `Retry-After` 等待由定时器唤醒，继续输入、离焦或重新打开面板不会递归重入保存过程，也不会提前发送请求。
- 摘录回访先校验受控批注，再切换到确切 PDF 文档或结构结果及原译文侧；对应页面或内容块加载后才显示已定位。未保存的笔记保留，异步 PDF 加载不会再次抢占页签。

## 验证与发布状态

发布源码：`65e00b49229b39ac9b9605b37e698f9202f894c1`，平台 `linux/amd64`。Web/test 构建中的 354 个应用与前端产物逐文件哈希一致，运行镜像已从 Docker Hub 按固定摘要回拉校验。

- 完整 Docker 非集成：577 passed＋84 subtests；前端类型、36 项单元、构建与 npm audit 通过。
- 候选镜像前端＋隔离真实 API/合成夹具：笔记 21 项、组合回归 8 项、统一界面 16 项，合计 45 项通过。测试容器使用测试依赖和本地假供应商；不宣称对正式论文发起了相同的写入测试。
- 四份 pip-audit 均为 0；Trivy 源码与三镜像无 HIGH/CRITICAL；三组件 SBOM 已生成，仅保存于私有证据。有效例外范围见源码提交对应的 [漏洞例外](https://github.com/ifzzh/iPaper/blob/65e00b49229b39ac9b9605b37e698f9202f894c1/security/vulnerability-exceptions.json)。
- 精确发布提交的 dev/main CI 均已通过（runs `35840774574` / `35842390634`，无原始报告附件）。

本轮验收不发起真实模型、MinerU、OCR 或翻译请求。
## 正式验收

Web 已按上述固定摘要部署，三个服务健康，`/readyz` 通过。正式 AutoSci 原文实际渲染 22 页，三条真实浏览器验收通过：慢保存期间继续输入及切换页签、冲突跨侧栏/页签仍冻结且决定期间的新输入保留、实际 Web 重启后的笔记与冲突副本恢复及 390px 阅读。

本轮笔记写入使用独立标识，清理前按 owner/paper/revision/正文逐条核验；已有论文、聊天、处理结果、阅读位置、阅读历史及账号偏好哈希保持不变，临时记录已清理。来源跨文档/语言侧与 429 等待的组合验证来自候选镜像前端及隔离夹具，不冒充正式数据场景。维护会话已撤销，Daily 原设置按字节恢复并恢复启用运行。两个 Worker 全程未重启，1897 个既有文件逐项哈希一致；备份与只切换 Web、保留当前数据的回退工具已就绪。

## 组件与回退

| 组件 | 固定镜像 |
| --- | --- |
| Web | `ifzzh520/ipaper:1.13.7@sha256:635e8a2e754b814d1a67af6f359989e452c5f862dc339a4d3aefb6bc6b14eb60` |
| Translation Worker | `ifzzh520/ipaper-translation-worker:1.2.0@sha256:a5fa3b4316802c15e99a7387baa49604166fb6092a1aca34b8c5e34de3d52003` |
| Document Worker | `ifzzh520/ipaper-document-worker:1.2.0@sha256:2c501ad68bdfc2e282518a813c46e548f598b1af85acc1069058741a45587298` |

回退基线为 Web 1.13.6＋Workers 1.2.0，保留当前数据库与已有笔记、冲突副本和来源记录；回退会重新暴露上述旧前端缺陷。未保存稿是浏览器会话内存，离开页面有提醒，不承诺浏览器重启后恢复。私人验收证据、备份、扫描 JSON 与 SBOM 不作为公开 Release 附件。
