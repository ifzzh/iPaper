# iPaper 1.5.0：论文元数据与入库识别

安全入库完成后即可阅读，公开书目补全在后台执行；联网失败不会移动文件或阻止阅读。详情支持完整作者、机构、发表信息、标识符和来源链接的修订编辑，以及对应当前信息的 BibTeX 复制和下载。人工修改与主动清空受保护，过期保存返回冲突并保留草稿。

单篇和跨页批量补全共用持久任务；取消停止后续请求，重启恢复未完成的只读查询。每篇最多八次书目请求，有全局限流和缓存。重复提示区分文件相同、同篇及相关版本，不自动删除或合并。元数据补全不调用模型、MinerU 或 OCR，不生成标签或主题分类。

旧标题编辑不再暗中搜索并覆盖其他信息，旧刷新端点不再重命名文件或移动资产。上传、URL、Daily 入库和导入保存使用同一事务保护；Daily 未入库发现记录不进入首次回填。作者摘要仍是论文元数据，既有 AI 概览、解读、双翻译、聊天、书签和阅读位置保持原有绑定。

## 证据与来源限制

本次建立 30 个有固定判断依据的合成身份样本，覆盖同名、参考文献 DOI、作者顺序、团体/中文作者、arXiv 版本、OpenReview 匿名/投稿和无文本等情况；这些是隔离测试，不是 30 篇真实论文的在线验收。使用已有许可明确的 PDF 样例验证 Worker 入库。

真实新客户端已核对 Crossref `10.1038/nature14539` 的标题、作者、期刊、年份、卷期页码，复读命中缓存、零新增请求。当前环境 arXiv 超时/429、DBLP 返回 HTML 验证页、OpenReview 返回 403；客户端如实拒绝异常内容，不绕过访问限制，不将其记为真实通过。用户可编辑可靠标识符、核对候选并重试；不得承诺所有论文都能自动补齐。详见[来源约束](../development/metadata.md)。

实际 Web 1.4.0 镜像完成隔离往返：旧 schema → 新书目及人工清空 → 旧镜像修改/新增论文 → 再升级；新旧用户修改、主动清空及文件路径均保留。补全进程在请求检查点退出后重新启动，已完成项不重跑。Edge 实测了详情、BibTeX 下载、跨页批次和两个标签的编辑冲突，保存 1920/1440/390 浅深视图。发布门禁和正式回填结果在交付后追加，当前文字不宣称尚未执行的部署通过。

## 组件与回退

本次仅发布 Web 1.5.0。两个 Worker 无运行代码或构建变化，继续复用 Translation 1.2.0 `sha256:a5fa3b4316802c15e99a7387baa49604166fb6092a1aca34b8c5e34de3d52003`、Document 1.2.0 `sha256:2c501ad68bdfc2e282518a813c46e548f598b1af85acc1069058741a45587298`，使用三个独立 ipaper 仓库；不新增 Worker latest。

数据库新增书目、字段修订、检查缓存、持久批次、来源证据及索引待同步事件；不重写旧表或迁移资产。停写一致性备份包含整个 SQLite 与已有不可变产物。回退前停止新补全任务，恢复本次部署前 Web 1.4.0、配置和渠道；继续使用当前数据库，保留新增表，不用旧库覆盖升级后的数据。再次升级协调回退期间的旧字段修改。

用法见[元数据说明](../development/metadata.md)，恢复见[一致性备份](../operations/structured-backup.md)。最终提交、Web digest、三组件 SBOM、安全检查、正式批次和回退位置随 [Release](https://github.com/ifzzh/iPaper/releases/tag/v1.5.0) 记录。


发布提交 `1c23b64f104e7a86c71c63da3ca7161f999ead6e`，平台 linux/amd64。Web 镜像已推送并回拉核验：`ifzzh520/ipaper:1.5.0@sha256:71180df0f1d388a70061898d2549e91372a75af2ac27aa284b25267d99cab341`。最终归档 Docker 387 项与84子测试通过；三组件安全门禁及SBOM通过。候选实际Worker入库、PDF2页渲染及Gunicorn重启后人工保护通过；正式部署与存量批次结果待下方实测记录补充。
